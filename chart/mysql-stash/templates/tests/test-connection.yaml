apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysql-stash.fullname" . }}-test-connection"
  labels:
{{ include "mysql-stash.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mysql-stash.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
